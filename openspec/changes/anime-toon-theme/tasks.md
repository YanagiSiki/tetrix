## 1. Clean Glassmorphic UI & Layout Refinement

- [x] 1.1 Update `style.css` with clean glassmorphic panels, refined dark midnight palette (`#090b14`), and elegant borders without skewed transforms.
- [x] 1.2 Update `index.html` and `src/ui.js` to restore clean headers ("TETRIX 3D — Anime Toon Edition"), dual-language labels (`SCORE // スコア`, `HIGH SCORE // ハイスコア`), and remove gimmicky popups.

## 2. Cel-Shaded 3D Renderer & Subtle VFX

- [x] 2.1 Refine `src/renderer.js` to render Cel-Shaded 3D blocks with clean black toon outlines and balanced lighting.
- [x] 2.2 Retain downward speed-line particles and subtle camera impact shake during hard drops in `src/renderer.js`.

## 3. Audio Polish & Verification

- [x] 3.1 Refine `src/audio.js` with crisp, pleasant sound synthesis and test `npm run build`.
